# Forward tests

这里保存 Skill 的真实前向测试，不随 `kevinbee-illustrations/` 安装。

- `article-body/v2.2-choice-release/`：正式示例“选择与放下”的原始生成文件；最终裁切版位于 Skill 的 `assets/article-examples/`。
- `article-body/v2.2-metaphor-variation/`：先写文章再配图的隐喻变化测试，包含文章、shot list、提示词、原图、最终裁切和测试报告。

测试中的 `images/final/` 只表示完成尺寸处理的测试输出，不等于已晋升为正式资产。测试结果只有在通过身份、因果参与、文章解释作用、画幅和系列变化检查后，才可以有选择地复制到 Skill 的 `assets/article-examples/`；测试目录本身不是参考资产池。
